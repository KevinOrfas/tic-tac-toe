import { createGame, getAllGames, getGameById, updatePlayer2, addMove, completeGame } from './gameRepository.js';

export { createGame, getAllGames, getGameById, updatePlayer2, addMove, completeGame };
